---
description: Execute a Vortex task through the unified refinement and delivery workflow.
argument-hint: <TASK-ID>
---

Execute the requested Vortex task using the Vortex skill's **Unified delivery
workflow**. Treat `$ARGUMENTS` as the readable task ID. Run the readiness
checks first, announce each stage, inspect and refine before editing, ask a
question only when indispensable, then implement, verify, open a PR, and move
the task to review. Never let pre-refinement block this workflow.
